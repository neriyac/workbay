# The load a chunk would do, for a machine whose chunk this client will never be sent.
# RemoteMachines feeds a hosted machine's saved tag to the copy an open screen is holding, and
# handleUpdateTag is the wrong door: it is a mod's own sync path and is free to ignore most of a
# tag, which Mekanism's does deliberately. This is the same method a chunk load calls.
public net.minecraft.world.level.block.entity.BlockEntity loadWithComponents(Lnet/minecraft/nbt/CompoundTag;Lnet/minecraft/core/HolderLookup$Provider;)V
